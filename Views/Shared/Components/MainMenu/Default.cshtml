@model IEnumerable<Page>

@foreach (var item in Model)
{
    @if (item.Description != "home")
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="" asp-controller="Pages" asp-action="Page" asp-route-slug="@item.Description">@item.Title</a>
        </li>
    }
}